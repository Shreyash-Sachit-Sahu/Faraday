fastapi
uvicorn[standard]
python-dotenv
qdrant-client>=1.13,<1.14
neo4j

# Phase 1 (torch installed separately with the CUDA index — see PHASE1.md)
sentence-transformers==3.2.1
datasets==3.1.0
rank_bm25==0.2.2
tqdm
cachetools
locust

# Phase 3 — knowledge graph (neo4j driver already present from Phase 0)
gliner==0.2.20
